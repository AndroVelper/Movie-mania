<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.Moviemania" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:statusBarColor">@color/white</item>
    </style>

    <style name="Theme.Moviemania" parent="Base.Theme.Moviemania" />
</resources>